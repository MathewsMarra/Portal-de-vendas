/**
 * Enumerador de operações do componente totvs-table. Este enumerador é utilizado para selecionar o tipo de operação
 * que a coluna efetuará;
 */
export declare enum TypeOperation {
    avg = 0,
    sum = 1,
    min = 2,
    max = 3,
}
