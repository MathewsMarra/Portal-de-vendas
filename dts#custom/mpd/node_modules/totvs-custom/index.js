import '@angular/core';
import '@angular/common';
import '@angular/common/http';
import '@ngx-translate/core';
import { TranslateHttpLoader } from '@ngx-translate/http-loader';
import { IonicPage, NavController, NavParams, Platform, ToastController, ViewController } from 'ionic-angular';
import { Storage } from '@ionic/storage';
import '@ionic-native/network';
import '@ionic-native/device';

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/**
 * @param {?} http
 * @return {?}
 */
function createTranslateLoader(http$$1) {
    return new TranslateHttpLoader(http$$1, './assets/i18n/', '.json');
}
var SharedModule = /** @class */ (function () {
    function SharedModule(translate) {
        translate.addLangs(['en', 'es', 'pt']);
        translate.setDefaultLang('en');
        var /** @type {?} */ browserLang = translate.getBrowserLang();
        translate.use(browserLang.match(/en|es|pt/) ? browserLang : 'en');
    }
    return SharedModule;
}());

var __extends = (undefined && undefined.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
var InfoModel = /** @class */ (function (_super) {
    __extends(InfoModel, _super);
    function InfoModel() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return InfoModel;
}(Object));
var MenuModel = /** @class */ (function (_super) {
    __extends(MenuModel, _super);
    function MenuModel() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return MenuModel;
}(Object));
var ListMenuModel = /** @class */ (function (_super) {
    __extends(ListMenuModel, _super);
    function ListMenuModel() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return ListMenuModel;
}(Object));
var DataModel = /** @class */ (function (_super) {
    __extends(DataModel, _super);
    function DataModel() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return DataModel;
}(Object));

var __extends$1 = (undefined && undefined.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var DateModel = /** @class */ (function (_super) {
    __extends$1(DateModel, _super);
    function DateModel() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return DateModel;
}(Object));

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var DateProvider = /** @class */ (function () {
    function DateProvider(date) {
        var /** @type {?} */ now;
        if (!date) {
            now = new Date();
        }
        else {
            now = date;
        }
        this.nowDate = now;
        this.now = this.format(now);
    }
    /**
     * @param {?} date
     * @return {?}
     */
    DateProvider.prototype.format = /**
     * @param {?} date
     * @return {?}
     */
    function (date) {
        var /** @type {?} */ dateFormated = new DateModel();
        dateFormated.day = this.formatDay(date.getDate());
        dateFormated.month = this.formatMonth(date.getMonth());
        dateFormated.year = date.getFullYear().toString();
        dateFormated.hour = this.formatHour(date.getHours());
        dateFormated.minute = this.formatMinute(date.getMinutes());
        dateFormated.second = this.formatSecond(date.getSeconds());
        return dateFormated;
    };
    /**
     * @param {?} day
     * @return {?}
     */
    DateProvider.prototype.formatDay = /**
     * @param {?} day
     * @return {?}
     */
    function (day) {
        if (day < 10) {
            return '0' + day.toString();
        }
        else {
            return day.toString();
        }
    };
    /**
     * @param {?} month
     * @return {?}
     */
    DateProvider.prototype.formatMonth = /**
     * @param {?} month
     * @return {?}
     */
    function (month) {
        if (++month < 10) {
            return '0' + month.toString();
        }
        else {
            return month.toString();
        }
    };
    /**
     * @param {?} hour
     * @return {?}
     */
    DateProvider.prototype.formatHour = /**
     * @param {?} hour
     * @return {?}
     */
    function (hour) {
        if (++hour < 10) {
            return '0' + hour.toString();
        }
        else {
            return hour.toString();
        }
    };
    /**
     * @param {?} minute
     * @return {?}
     */
    DateProvider.prototype.formatMinute = /**
     * @param {?} minute
     * @return {?}
     */
    function (minute) {
        if (++minute < 10) {
            return '0' + minute.toString();
        }
        else {
            return minute.toString();
        }
    };
    /**
     * @param {?} second
     * @return {?}
     */
    DateProvider.prototype.formatSecond = /**
     * @param {?} second
     * @return {?}
     */
    function (second) {
        if (++second < 10) {
            return '0' + second.toString();
        }
        else {
            return second.toString();
        }
    };
    /**
     * @return {?}
     */
    DateProvider.prototype.getDateModel = /**
     * @return {?}
     */
    function () {
        return this.now;
    };
    /**
     * @param {?} format
     * @return {?}
     */
    DateProvider.prototype.getDateFormated = /**
     * @param {?} format
     * @return {?}
     */
    function (format) {
        var /** @type {?} */ dateFormated = format;
        dateFormated = dateFormated.replace('D', (this.now.day) ? this.now.day : '');
        dateFormated = dateFormated.replace('M', (this.now.month) ? this.now.month : '');
        dateFormated = dateFormated.replace('Y', (this.now.year) ? this.now.year : '');
        dateFormated = dateFormated.replace('h', (this.now.hour) ? this.now.hour : '');
        dateFormated = dateFormated.replace('m', (this.now.minute) ? this.now.minute : '');
        dateFormated = dateFormated.replace('s', (this.now.second) ? this.now.second : '');
        return dateFormated;
    };
    return DateProvider;
}());

var __extends$2 = (undefined && undefined.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var UserModel = /** @class */ (function (_super) {
    __extends$2(UserModel, _super);
    function UserModel() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return UserModel;
}(Object));

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var AppProvider = /** @class */ (function () {
    function AppProvider(app, platform, loadingCtrl, storage$$1, network$$1, toastCtrl, alertCtrl, modalCtrl, device$$1) {
        var _this = this;
        this.app = app;
        this.platform = platform;
        this.loadingCtrl = loadingCtrl;
        this.storage = storage$$1;
        this.network = network$$1;
        this.toastCtrl = toastCtrl;
        this.alertCtrl = alertCtrl;
        this.modalCtrl = modalCtrl;
        this.device = device$$1;
        this.storage = new Storage({
            name: this.database,
            driverOrder: this.databaseDriverOrder,
            storeName: this.table
        });
        //charge info default
        this.info = new InfoModel();
        this.storage.ready().then(function (data) {
            //load info in database
            var /** @type {?} */ config = {
                time_without_syncing: 0,
                proxy: (_this.runInBrowser()) ? true : false,
                proxy_url: '177.137.233.106',
                proxy_prt: 8080,
                proxy_res: 'proxy.php',
                port_ws: 40,
                port_pt: 41
            };
            _this.load().then(function (data) {
                _this.setData({ key: 'name', value: 'PDI' });
                _this.setData({ key: 'version', value: '0.0.1' });
                _this.setData({ key: 'current_date', value: _this.datePrvd.getDateFormated('YMD') });
                _this.setData({ key: 'current_user', value: _this.guest() });
                _this.setData({ key: 'path', value: 'restcom' });
                _this.setData({ key: 'port', value: 9909 });
                _this.setData({ key: 'config', value: config });
            });
        });
    }
    /**
     * @return {?}
     */
    AppProvider.prototype.runInBrowser = /**
     * @return {?}
     */
    function () {
        return document.URL.startsWith('http');
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.isTest = /**
     * @return {?}
     */
    function () {
        return document.URL.indexOf('http://127.0.0.1:81') >= 0 || document.URL.indexOf('http://localhost:81') >= 0;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getInfoDevice = /**
     * @return {?}
     */
    function () {
        return this.device;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getInfoFormater = /**
     * @return {?}
     */
    function () {
        return {
            currency_code: 'BRL',
            currency_expression: '1.2-2',
            number_decimal: '2',
        };
    };
    /**
     * @param {?} key
     * @return {?}
     */
    AppProvider.prototype.is = /**
     * @param {?} key
     * @return {?}
     */
    function (key) {
        switch (key) {
            case 'mobile':
                return this.platform.width() <= 576;
            case 'tablet':
                return this.platform.width() <= 768;
            case 'desktop':
                return this.platform.width() <= 992;
            default:
                return true;
        }
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getInfo = /**
     * @return {?}
     */
    function () {
        return this.info;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getConfig = /**
     * @return {?}
     */
    function () {
        return this.info.config;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getDebugInfo = /**
     * @return {?}
     */
    function () {
        return JSON.stringify(this.info);
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.isSync = /**
     * @return {?}
     */
    function () {
        return this.info.sync;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getDate = /**
     * @return {?}
     */
    function () {
        return this.info.current_date;
    };
    /**
     * @param {?} format
     * @return {?}
     */
    AppProvider.prototype.getDateFormated = /**
     * @param {?} format
     * @return {?}
     */
    function (format) {
        var /** @type {?} */ dateFormated = format;
        dateFormated = dateFormated.replace('D', this.info.current_date.substr(6, 2));
        dateFormated = dateFormated.replace('M', this.info.current_date.substr(4, 2));
        dateFormated = dateFormated.replace('Y', this.info.current_date.substr(0, 4));
        return dateFormated;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getUrlProxy = /**
     * @return {?}
     */
    function () {
        if (this.info.config.proxy) {
            var /** @type {?} */ url = void 0;
            url = this.info.config.proxy_url;
            if (url.indexOf('http') === -1) {
                url = 'http://' + url;
            }
            if (this.info.config.proxy_prt) {
                url += ':' + this.info.config.proxy_prt.toString();
            }
            if (this.info.config.proxy_res) {
                url += '/' + this.info.config.proxy_res;
            }
            return url;
        }
        else {
            throw 'Error proxy deactivate!';
        }
    };
    /**
     * @param {?} resource
     * @param {?=} type
     * @param {?=} action
     * @return {?}
     */
    AppProvider.prototype.getUrl = /**
     * @param {?} resource
     * @param {?=} type
     * @param {?=} action
     * @return {?}
     */
    function (resource, type, action) {
        var /** @type {?} */ url;
        url = this.info.server;
        if (url.indexOf('http') === -1) {
            url = 'http://' + url;
        }
        if (type) {
            if (type === 'ws') {
                url += ':' + this.info.config.port_ws.toString();
            }
            else {
                url += ':' + this.info.config.port_pt.toString();
            }
        }
        else {
            if (this.info.port) {
                url += ':' + this.info.port.toString();
            }
        }
        if (type && !action) {
            if (type === 'ws') {
                url += '/ws';
            }
            else {
                url += '/pp_' + this.info.company;
            }
        }
        else {
            if (this.info.path) {
                url += '/' + this.info.path;
            }
        }
        return url + '/' + resource;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getConnection = /**
     * @return {?}
     */
    function () {
        if (!this.runInBrowser()) {
            return (this.network.type) ? this.network.type.toUpperCase() : 'NONE';
        }
        else {
            return 'UNKNOW';
        }
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.infoNetwork = /**
     * @return {?}
     */
    function () {
        this.presentAlert('Connection', this.getConnection());
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.isOnline = /**
     * @return {?}
     */
    function () {
        if (!this.runInBrowser()) {
            return this.getConnection() !== 'NONE';
        }
        else {
            return true;
        }
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getDatabase = /**
     * @return {?}
     */
    function () {
        return this.database;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getDatabaseDriverOrder = /**
     * @return {?}
     */
    function () {
        return this.databaseDriverOrder;
    };
    /**
     * @param {?=} user
     * @return {?}
     */
    AppProvider.prototype.setUser = /**
     * @param {?=} user
     * @return {?}
     */
    function (user) {
        if (user) {
            this.info.current_user = user;
        }
        else {
            this.info.current_user = this.guest();
        }
        this.save();
    };
    /**
     * @param {?} company
     * @return {?}
     */
    AppProvider.prototype.setCompany = /**
     * @param {?} company
     * @return {?}
     */
    function (company) {
        this.info.company = company;
        this.save();
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getUser = /**
     * @return {?}
     */
    function () {
        if (this.info.current_user) {
            return this.info.current_user;
        }
        else {
            return {};
        }
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.guest = /**
     * @return {?}
     */
    function () {
        var /** @type {?} */ guest = new UserModel();
        guest.username = 'guest';
        guest.name = 'Guest';
        return guest;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.isGuest = /**
     * @return {?}
     */
    function () {
        if (this.getUser().hasOwnProperty('username')) {
            return this.getUser().username === this.guest().username;
        }
        else {
            return true;
        }
    };
    /**
     * @param {?} key
     * @param {?} pass
     * @return {?}
     */
    AppProvider.prototype.crypt = /**
     * @param {?} key
     * @param {?} pass
     * @return {?}
     */
    function (key, pass) {
        key = key + 'RC4PLANCRYPTO';
        var /** @type {?} */ s = [];
        var /** @type {?} */ j = 0;
        var /** @type {?} */ i = 0;
        var /** @type {?} */ x;
        var /** @type {?} */ res = '';
        for (var /** @type {?} */ i_1 = 0; i_1 < 256; i_1++) {
            s[i_1] = i_1;
        }
        for (var /** @type {?} */ i_2 = 0; i_2 < 256; i_2++) {
            j = (j + s[i_2] + key.charCodeAt(i_2 % key.length)) % 256;
            x = s[i_2];
            s[i_2] = s[j];
            s[j] = x;
        }
        i = 0;
        j = 0;
        for (var /** @type {?} */ y = 0; y < pass.length; y++) {
            i = (i + 1) % 256;
            j = (j + s[i]) % 256;
            x = s[i];
            s[i] = s[j];
            s[j] = x;
            // tslint:disable-next-line:no-bitwise
            res += String.fromCharCode(pass.charCodeAt(y) ^ s[(s[i] + s[j]) % 256]);
        }
        return res;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.logout = /**
     * @return {?}
     */
    function () {
        this.app.getRootNav().setRoot('LoginPage');
    };
    /**
     * @param {?} data
     * @return {?}
     */
    AppProvider.prototype.setData = /**
     * @param {?} data
     * @return {?}
     */
    function (data) {
        this.info[data.key] = data.value;
        this.save();
    };
    /**
     * @param {?} data
     * @return {?}
     */
    AppProvider.prototype.getData = /**
     * @param {?} data
     * @return {?}
     */
    function (data) {
        return this.storage.get(data.key);
    };
    /**
     * @param {?} menu
     * @return {?}
     */
    AppProvider.prototype.setMenu = /**
     * @param {?} menu
     * @return {?}
     */
    function (menu) {
        this.menu = menu;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.getMenu = /**
     * @return {?}
     */
    function () {
        return this.menu;
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.load = /**
     * @return {?}
     */
    function () {
        var _this = this;
        return this.storage.forEach(function (value, key, int) {
            _this.info[key] = value;
        });
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.save = /**
     * @return {?}
     */
    function () {
        for (var /** @type {?} */ key in this.info) {
            if (this.info.hasOwnProperty(key)) {
                this.storage.set(key, this.info[key]);
            }
        }
    };
    /**
     * @param {?} title
     * @param {?} msg
     * @return {?}
     */
    AppProvider.prototype.presentAlert = /**
     * @param {?} title
     * @param {?} msg
     * @return {?}
     */
    function (title, msg) {
        var /** @type {?} */ alert = this.alertCtrl.create({
            title: title,
            subTitle: msg,
            buttons: ['Close']
        });
        alert.present();
    };
    /**
     * @param {?} msg
     * @return {?}
     */
    AppProvider.prototype.presentToast = /**
     * @param {?} msg
     * @return {?}
     */
    function (msg) {
        var /** @type {?} */ toast = this.toastCtrl.create({
            message: msg,
            duration: 6000,
            showCloseButton: true,
        });
        toast.present();
    };
    /**
     * @param {?} page
     * @param {?=} params
     * @param {?=} fn
     * @return {?}
     */
    AppProvider.prototype.presentModal = /**
     * @param {?} page
     * @param {?=} params
     * @param {?=} fn
     * @return {?}
     */
    function (page, params, fn) {
        if (!params) {
            params = {};
        }
        var /** @type {?} */ modal = this.modalCtrl.create(page, params);
        if (fn) {
            modal.onDidDismiss(fn);
        }
        modal.present();
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.startLoading = /**
     * @return {?}
     */
    function () {
        this.loading = this.loadingCtrl.create({
            content: 'Please wait...'
        });
        this.loading.present();
    };
    /**
     * @return {?}
     */
    AppProvider.prototype.stopLoading = /**
     * @return {?}
     */
    function () {
        this.loading.dismiss();
    };
    /**
     * \@description Função para converter XML em JSON, realizada alteração para lelet em consideração ou não os atributos e inputar no objeto também os valores dos nós
     * @author David Walsh
     * \@source https://davidwalsh.name/convert-xml-json
     * @param {?} xml
     * @param {?=} attr
     * @return {?}
     */
    AppProvider.prototype.parseToJSON = /**
     * \@description Função para converter XML em JSON, realizada alteração para lelet em consideração ou não os atributos e inputar no objeto também os valores dos nós
     * @author David Walsh
     * \@source https://davidwalsh.name/convert-xml-json
     * @param {?} xml
     * @param {?=} attr
     * @return {?}
     */
    function (xml, attr) {
        var /** @type {?} */ json = {};
        if (xml.nodeType === 1) {
            if (xml.attributes.length > 0 && attr) {
                for (var /** @type {?} */ j = 0; j < xml.attributes.length; j++) {
                    var /** @type {?} */ attribute = xml.attributes.item(j);
                    json[attribute.nodeName] = attribute.nodeValue;
                }
            }
        }
        else if (xml.nodeType === 3) {
            json = xml.nodeValue;
        }
        if (xml.childNodes.length > 0) {
            for (var /** @type {?} */ i = 0; i < xml.childNodes.length; i++) {
                var /** @type {?} */ item = xml.childNodes.item(i);
                var /** @type {?} */ nodeName = item.nodeName;
                if (typeof (json[nodeName]) === 'undefined') {
                    if (item.firstChild == null || (item.firstChild && item.firstChild.nodeType === 3)) {
                        if (item.firstChild) {
                            json[nodeName] = item.firstChild.nodeValue;
                        }
                        else {
                            json[nodeName] = '';
                        }
                    }
                    else {
                        json[nodeName] = this.parseToJSON(item);
                    }
                }
                else {
                    if (typeof (json[nodeName].push) === 'undefined') {
                        var /** @type {?} */ old = json[nodeName];
                        json[nodeName] = [];
                        json[nodeName].push(old);
                    }
                    json[nodeName].push(this.parseToJSON(item));
                }
            }
        }
        return json;
    };
    return AppProvider;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var FormaterProvider = /** @class */ (function () {
    function FormaterProvider(currencyPipe, decimalPipe, percentPipe, datePipe) {
        this.currencyPipe = currencyPipe;
        this.decimalPipe = decimalPipe;
        this.percentPipe = percentPipe;
        this.datePipe = datePipe;
    }
    /**
     * @param {?} value
     * @return {?}
     */
    FormaterProvider.prototype.setValue = /**
     * @param {?} value
     * @return {?}
     */
    function (value) {
        this.value = value;
    };
    /**
     * @return {?}
     */
    FormaterProvider.prototype.toString = /**
     * @return {?}
     */
    function () {
        return this.value.toString();
    };
    /**
     * @return {?}
     */
    FormaterProvider.prototype.toInt = /**
     * @return {?}
     */
    function () {
        return Number.parseInt(this.toString());
    };
    /**
     * @param {?=} fixed
     * @return {?}
     */
    FormaterProvider.prototype.toFloat = /**
     * @param {?=} fixed
     * @return {?}
     */
    function (fixed) {
        if (fixed) {
            return Number.parseFloat(Number.parseFloat(this.toString()).toFixed(fixed));
        }
        return Number.parseFloat(this.toString());
    };
    /**
     * @param {?=} currencyCode
     * @param {?=} symbolDisplay
     * @param {?=} digits
     * @return {?}
     */
    FormaterProvider.prototype.toCurrency = /**
     * @param {?=} currencyCode
     * @param {?=} symbolDisplay
     * @param {?=} digits
     * @return {?}
     */
    function (currencyCode, symbolDisplay, digits) {
        return this.currencyPipe.transform(this.toString(), currencyCode, symbolDisplay, digits);
    };
    /**
     * @param {?=} digits
     * @return {?}
     */
    FormaterProvider.prototype.toDecimal = /**
     * @param {?=} digits
     * @return {?}
     */
    function (digits) {
        return this.decimalPipe.transform(this.toString(), digits);
    };
    /**
     * @param {?=} digits
     * @return {?}
     */
    FormaterProvider.prototype.toPercent = /**
     * @param {?=} digits
     * @return {?}
     */
    function (digits) {
        return this.percentPipe.transform(this.toString(), digits);
    };
    /**
     * @param {?=} pattern
     * @return {?}
     */
    FormaterProvider.prototype.toDateFormated = /**
     * @param {?=} pattern
     * @return {?}
     */
    function (pattern) {
        return this.datePipe.transform(this.value, pattern);
    };
    return FormaterProvider;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/**
 * \@color <br/><br/>
 * Este atributo é responsável por atribuir a cor principal do componente. Por padrão este atributo está preenchido com o valor `primary`.
 * Deve ser preenchido com valores do tipo `string`.
 *
 * \@isPage <br/><br/>
 * Este atributo é responsável por definir se o componente será utilizado como página ou como uma janela modal. Por padrão este atributo está preenchido com o valor
 * `false`. Deve ser preenchido com valores do tipo `boolean`.
 *
 * ### Exemplo HTML 5
 * -------------------------------------------------------------------------------------------------
 *  `<ion-header text-center>`<br/>
 *      `<totvs-header isPage="true" color="primary">Titulo do Header`<br/>
 *       `</totvs-header>`<br/>
 *  `</ion-header>`
 * -------------------------------------------------------------------------------------------------
 *
 */
var HeaderComponent = /** @class */ (function () {
    /**
     * Construtor do componente
     * @param platform Injeção de dependencia responsável por manipular os metodos de tamanho de tela (screen resolution)
     * @param network Injeção de dependencia responsável por manipular os metodos de rede e conexão.
     * @param alertCtrl Injeção de dependencia responsável por manipular as mensagens de alerta
     * @param viewCtrl Injeção de dependencia responsável por manipular os metodos da tela
     */
    function HeaderComponent(platform, network$$1, alertCtrl, viewCtrl) {
        this.platform = platform;
        this.network = network$$1;
        this.alertCtrl = alertCtrl;
        this.viewCtrl = viewCtrl;
    }
    /**
     * Metodo responsável por obter as conexões e rede
     * @return {?}
     */
    HeaderComponent.prototype.getConnection = /**
     * Metodo responsável por obter as conexões e rede
     * @return {?}
     */
    function () {
        if (this.platform.is('cordova')) {
            return (this.network.type) ? this.network.type.toUpperCase() : 'NONE';
        }
        else {
            return 'UNKNOW';
        }
    };
    /**
     * Metodo responsável por exibir as informações de rede
     * @return {?}
     */
    HeaderComponent.prototype.infoNetwork = /**
     * Metodo responsável por exibir as informações de rede
     * @return {?}
     */
    function () {
        this.presentAlert('Connection', this.getConnection());
    };
    /**
     * Metodo responsável por verificar se está em rede ou não.
     * @return {?}
     */
    HeaderComponent.prototype.isOnline = /**
     * Metodo responsável por verificar se está em rede ou não.
     * @return {?}
     */
    function () {
        if (this.platform.is('cordova')) {
            return this.getConnection() !== 'NONE';
        }
        else {
            return true;
        }
    };
    /**
     * Metodo responsável por exibir alertas em tela
     * @param {?} title //Parametro responsável por atribuir o titulo a janela de alerta
     * @param {?} msg //Parametro responsável por atribuir a mensagem da janela de alerta
     * @return {?}
     */
    HeaderComponent.prototype.presentAlert = /**
     * Metodo responsável por exibir alertas em tela
     * @param {?} title //Parametro responsável por atribuir o titulo a janela de alerta
     * @param {?} msg //Parametro responsável por atribuir a mensagem da janela de alerta
     * @return {?}
     */
    function (title, msg) {
        var /** @type {?} */ alert = this.alertCtrl.create({
            title: title,
            subTitle: msg,
            buttons: ['Close']
        });
        alert.present();
    };
    /**
     * Metodo responsável por fechar a janela modal
     */
    /**
     * Metodo responsável por fechar a janela modal
     * @return {?}
     */
    HeaderComponent.prototype.dismiss = /**
     * Metodo responsável por fechar a janela modal
     * @return {?}
     */
    function () {
        this.viewCtrl.dismiss();
    };
    return HeaderComponent;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var HeaderComponentModule = /** @class */ (function () {
    function HeaderComponentModule() {
    }
    /**
     * @return {?}
     */
    HeaderComponentModule.forRoot = /**
     * @return {?}
     */
    function () {
        return {
            ngModule: HeaderComponentModule,
            providers: []
        };
    };
    return HeaderComponentModule;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/** @enum {number} */
var TypeColumnSelect = {
    string: 0,
    number: 1,
    date: 2,
    object: 3,
    array: 4,
    default: 5,
};
TypeColumnSelect[TypeColumnSelect.string] = "string";
TypeColumnSelect[TypeColumnSelect.number] = "number";
TypeColumnSelect[TypeColumnSelect.date] = "date";
TypeColumnSelect[TypeColumnSelect.object] = "object";
TypeColumnSelect[TypeColumnSelect.array] = "array";
TypeColumnSelect[TypeColumnSelect.default] = "default";

var __extends$3 = (undefined && undefined.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/**
 * Modelo de classe utilizado para inserção de itens no componente totvs-select
 */
var ItemSelect = /** @class */ (function (_super) {
    __extends$3(ItemSelect, _super);
    function ItemSelect() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return ItemSelect;
}(Object));
var EditSelect = /** @class */ (function (_super) {
    __extends$3(EditSelect, _super);
    function EditSelect() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return EditSelect;
}(Object));

var __extends$4 = (undefined && undefined.__extends) || (function () {
    var extendStatics = Object.setPrototypeOf ||
        ({ __proto__: [] } instanceof Array && function (d, b) { d.__proto__ = b; }) ||
        function (d, b) { for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p]; };
    return function (d, b) {
        extendStatics(d, b);
        function __() { this.constructor = d; }
        d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
    };
})();
/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/**
 * Modelo responsável por preencher as colunas do componente totvs-select
 */
var ColumnSelect = /** @class */ (function (_super) {
    __extends$4(ColumnSelect, _super);
    function ColumnSelect() {
        return _super !== null && _super.apply(this, arguments) || this;
    }
    return ColumnSelect;
}(Object));

var __decorate = (undefined && undefined.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (undefined && undefined.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var SelectComponent = /** @class */ (function () {
    /**
     * @param navCtrl Injeção de dependencia responsável pela navegação
     * @param navParams Injeção de dependencia responsavel pelos parametros
     * @param toastCtrl Injeção de dependencia responsavel pelas mensagens
     * @param platform  Injeção de dependencia responsável por verificar a plataforma
     * @param viewCtrl Injeção de dependencia responsável pela view
     */
    function SelectComponent(navCtrl, navParams, toastCtrl, platform, viewCtrl) {
        this.navCtrl = navCtrl;
        this.navParams = navParams;
        this.toastCtrl = toastCtrl;
        this.platform = platform;
        this.viewCtrl = viewCtrl;
        var /** @type {?} */ items = this.navParams.get('items');
        var /** @type {?} */ columns = this.navParams.get('columns');
        var /** @type {?} */ multiSelect = this.navParams.get('multi_select');
        if (items != null && columns != null && multiSelect != null) {
            this.columns = columns;
            this.items = items;
            this.multiSelect = multiSelect;
        }
        if (this.multiSelect) {
            this.title = 'Choose items';
        }
        else {
            this.title = 'Choose an item';
        }
    }
    /**
     * Esta função é responsável por retornar o valor do parametro multiSelect que indica se poderá selecionar apenas um ou vários objetos
     */
    /**
     * Esta função é responsável por retornar o valor do parametro multiSelect que indica se poderá selecionar apenas um ou vários objetos
     * @return {?}
     */
    SelectComponent.prototype.hasMultiple = /**
     * Esta função é responsável por retornar o valor do parametro multiSelect que indica se poderá selecionar apenas um ou vários objetos
     * @return {?}
     */
    function () {
        return this.multiSelect;
    };
    /**
   * Metodo responsável por realizar a busca dos itens
   */
    /**
     * Metodo responsável por realizar a busca dos itens
     * @param {?} value
     * @return {?}
     */
    SelectComponent.prototype.getItems = /**
     * Metodo responsável por realizar a busca dos itens
     * @param {?} value
     * @return {?}
     */
    function (value) {
        if (value && value.trim() !== '' && this.getItems.length > 0) {
            var /** @type {?} */ search_1 = value.toLowerCase();
            var /** @type {?} */ filtered_1 = this.filterColumnSearch();
            this.clearFilteredItems();
            this.filteredItems = this.items
                .filter((function (d) {
                for (var /** @type {?} */ i = 0; i < filtered_1.length; i++) {
                    if (d[filtered_1[i].attribute].toString().toLowerCase().indexOf(search_1) >= 0 || value === '*') {
                        return true;
                    }
                }
                return false;
            }));
            return this.filteredItems;
        }
        else {
            return this.clearFilteredItems();
        }
    };
    /**
   * Metodo responsável por retornar se a lista está com itens ou não
   */
    /**
     * Metodo responsável por retornar se a lista está com itens ou não
     * @return {?}
     */
    SelectComponent.prototype.hasItemsAdded = /**
     * Metodo responsável por retornar se a lista está com itens ou não
     * @return {?}
     */
    function () {
        return this.itemsAdded.length > 0;
    };
    /**
   * Metodo responsável por verificar se existe algum item selecionado ou não na lista de itens filtrados
   */
    /**
     * Metodo responsável por verificar se existe algum item selecionado ou não na lista de itens filtrados
     * @return {?}
     */
    SelectComponent.prototype.hasItemSelected = /**
     * Metodo responsável por verificar se existe algum item selecionado ou não na lista de itens filtrados
     * @return {?}
     */
    function () {
        var /** @type {?} */ flag = false;
        this.filteredItems.forEach(function (item) {
            if (item.selected === true) {
                flag = true;
            }
        });
        return flag;
    };
    /**
   * Metodo responsável por "amarrar" item a coluna do item
   */
    /**
     * Metodo responsável por "amarrar" item a coluna do item
     * @param {?} column
     * @param {?} item
     * @return {?}
     */
    SelectComponent.prototype.getItemsPerColumn = /**
     * Metodo responsável por "amarrar" item a coluna do item
     * @param {?} column
     * @param {?} item
     * @return {?}
     */
    function (column, item) {
        if (column.editable) {
            return item[column.attribute].value;
        }
        else {
            return item[column.attribute];
        }
    };
    /**
   * Metodo responsavel por retornar as colunas que serão utilizadas como critério de busca
   */
    /**
     * Metodo responsavel por retornar as colunas que serão utilizadas como critério de busca
     * @return {?}
     */
    SelectComponent.prototype.filterColumnSearch = /**
     * Metodo responsavel por retornar as colunas que serão utilizadas como critério de busca
     * @return {?}
     */
    function () {
        var /** @type {?} */ result = [];
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (this.columns[i].filterable) {
                result.push(this.columns[i]);
            }
        }
        return result;
    };
    /**
     * Este metodo é responsável por limpar a lista de items filtrados
     * @return {?}
     */
    SelectComponent.prototype.clearFilteredItems = /**
     * Este metodo é responsável por limpar a lista de items filtrados
     * @return {?}
     */
    function () {
        return this.filteredItems = [];
    };
    /**
     * Metodo responsavel por selecionar todos ou desmarcar todos os items
     * @param ev Parametro recebe o estado do checkbox para selecionar todos os items
     */
    /**
     * Metodo responsavel por selecionar todos ou desmarcar todos os items
     * @return {?}
     */
    SelectComponent.prototype.selectAll = /**
     * Metodo responsavel por selecionar todos ou desmarcar todos os items
     * @return {?}
     */
    function () {
        var _this = this;
        this.selectedAll = !this.selectedAll;
        var /** @type {?} */ aux = this.filteredItems;
        aux.forEach(function (i) { return i.selected = _this.selectedAll; });
        this.filteredItems = aux;
    };
    /**
     * Metodo responsável por verificar se todos os items estão selecionados
     */
    /**
     * Metodo responsável por verificar se todos os items estão selecionados
     * @return {?}
     */
    SelectComponent.prototype.allChecked = /**
     * Metodo responsável por verificar se todos os items estão selecionados
     * @return {?}
     */
    function () {
        return this.selectedAll;
    };
    /**
   * Metodo responsável por adicionar o item a lista de itens selecionados
   */
    /**
     * Metodo responsável por adicionar o item a lista de itens selecionados
     * @param {?} ev
     * @param {?} item
     * @return {?}
     */
    SelectComponent.prototype.addItem = /**
     * Metodo responsável por adicionar o item a lista de itens selecionados
     * @param {?} ev
     * @param {?} item
     * @return {?}
     */
    function (ev, item) {
        item.selected = ev.checked;
        if (this.hasMultiple()) {
            this.itemsAdded.push(item);
            this.items = this.items.filter(function (i) { return i !== item; });
            this.filteredItems = this.filteredItems.filter(function (i) { return i !== item; });
        }
        else {
            this.clearFilteredItems();
            this.viewCtrl.dismiss(item);
            this.toastMessage('Item added successfully');
        }
    };
    /**
   * Metodo responsavel por remover o item da lista de itens selecionados
   */
    /**
     * Metodo responsavel por remover o item da lista de itens selecionados
     * @param {?} item
     * @return {?}
     */
    SelectComponent.prototype.removeItem = /**
     * Metodo responsavel por remover o item da lista de itens selecionados
     * @param {?} item
     * @return {?}
     */
    function (item) {
        item.selected = false;
        this.itemsAdded = this.itemsAdded.filter(function (i) { return i !== item; });
        this.items.push(item);
    };
    /**
   * Metodo responsável por fechar a tela e retornar a lista de itens selecionados
   */
    /**
     * Metodo responsável por fechar a tela e retornar a lista de itens selecionados
     * @return {?}
     */
    SelectComponent.prototype.dismissMiltiple = /**
     * Metodo responsável por fechar a tela e retornar a lista de itens selecionados
     * @return {?}
     */
    function () {
        this.viewCtrl.dismiss(this.itemsAdded);
    };
    /**
   * Metodo responsável por indicar se existe somente um item na lista
   */
    /**
     * Metodo responsável por indicar se existe somente um item na lista
     * @return {?}
     */
    SelectComponent.prototype.hasOneItemAdded = /**
     * Metodo responsável por indicar se existe somente um item na lista
     * @return {?}
     */
    function () {
        return this.itemsAdded.length === 1;
    };
    /**
   * Metodo responsável por retornar o item selecionado ao campo ao qual ele está atrelado
   */
    /**
     * Metodo responsável por retornar o item selecionado ao campo ao qual ele está atrelado
     * @return {?}
     */
    SelectComponent.prototype.dismissSingle = /**
     * Metodo responsável por retornar o item selecionado ao campo ao qual ele está atrelado
     * @return {?}
     */
    function () {
        if (!this.multiSelect) {
            if (this.itemsAdded.length === 1) {
                this.viewCtrl.dismiss(this.itemsAdded);
            }
            else {
                this.toastMessage('Select only one item');
            }
        }
    };
    /**
     Metodo responsável pela emissão de mensagens nesta tela
     */
    /**
     * Metodo responsável pela emissão de mensagens nesta tela
     * @param {?} message
     * @return {?}
     */
    SelectComponent.prototype.toastMessage = /**
     * Metodo responsável pela emissão de mensagens nesta tela
     * @param {?} message
     * @return {?}
     */
    function (message) {
        var /** @type {?} */ msg = this.toastCtrl.create({
            message: message,
            duration: 1000,
            position: 'bottom'
        });
        msg.present();
    };
    /**
     * Metodo responsável por verificar se existem itens ou não na lista de filtros
     */
    /**
     * Metodo responsável por verificar se existem itens ou não na lista de filtros
     * @return {?}
     */
    SelectComponent.prototype.hasItemsFiltered = /**
     * Metodo responsável por verificar se existem itens ou não na lista de filtros
     * @return {?}
     */
    function () {
        return this.filteredItems.length > 0;
    };
    /**
   * Metodo  responsável por verificar se o tamanho da tela é menor ou igual ao valor definido na variável **this.widthPixel**.
   */
    /**
     * Metodo  responsável por verificar se o tamanho da tela é menor ou igual ao valor definido na variável **this.widthPixel**.
     * @return {?}
     */
    SelectComponent.prototype.formatTableScreen = /**
     * Metodo  responsável por verificar se o tamanho da tela é menor ou igual ao valor definido na variável **this.widthPixel**.
     * @return {?}
     */
    function () {
        return this.platform.width() <= this.widthPixel;
    };
    /**
     * @param {?} column
     * @return {?}
     */
    SelectComponent.prototype.hasEdit = /**
     * @param {?} column
     * @return {?}
     */
    function (column) {
        return column.editable;
    };
    /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é string
     * @param column este parametro é preenchido com a coluna a ser verificada
     * @param item este parametro é preenchido com o item a ser verificado
     */
    /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é string
     * @param {?} column este parametro é preenchido com a coluna a ser verificada
     * @param {?} item este parametro é preenchido com o item a ser verificado
     * @return {?}
     */
    SelectComponent.prototype.hasString = /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é string
     * @param {?} column este parametro é preenchido com a coluna a ser verificada
     * @param {?} item este parametro é preenchido com o item a ser verificado
     * @return {?}
     */
    function (column, item) {
        if (item.hasOwnProperty(column.attribute)) {
            return item[column.attribute].type === TypeColumnSelect.string;
        }
        else {
            return false;
        }
    };
    /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é numerico
     * @param column este parametro é preenchido com a coluna a ser verificada
     * @param item este parametro é preenchido com o item a ser verificado
     */
    /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é numerico
     * @param {?} column este parametro é preenchido com a coluna a ser verificada
     * @param {?} item este parametro é preenchido com o item a ser verificado
     * @return {?}
     */
    SelectComponent.prototype.hasNumber = /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é numerico
     * @param {?} column este parametro é preenchido com a coluna a ser verificada
     * @param {?} item este parametro é preenchido com o item a ser verificado
     * @return {?}
     */
    function (column, item) {
        if (item.hasOwnProperty(column.attribute)) {
            return item[column.attribute].type === TypeColumnSelect.number;
        }
        else {
            return false;
        }
    };
    /**
   * Metodo responsavel por atrelar item a coluna verificando se o tipo é data
   * @param column este parametro é preenchido com a coluna a ser verificada
   * @param item este parametro é preenchido com o item a ser verificado
   */
    /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é data
     * @param {?} column este parametro é preenchido com a coluna a ser verificada
     * @param {?} item este parametro é preenchido com o item a ser verificado
     * @return {?}
     */
    SelectComponent.prototype.hasDate = /**
     * Metodo responsavel por atrelar item a coluna verificando se o tipo é data
     * @param {?} column este parametro é preenchido com a coluna a ser verificada
     * @param {?} item este parametro é preenchido com o item a ser verificado
     * @return {?}
     */
    function (column, item) {
        if (item.hasOwnProperty(column.attribute)) {
            return item[column.attribute].type === TypeColumnSelect.date;
        }
        else {
            return false;
        }
    };
    SelectComponent = __decorate([
        IonicPage(),
        __metadata("design:paramtypes", [NavController, NavParams, ToastController, Platform, ViewController])
    ], SelectComponent);
    return SelectComponent;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var SelectComponentModule = /** @class */ (function () {
    function SelectComponentModule() {
    }
    /**
     * @return {?}
     */
    SelectComponentModule.forRoot = /**
     * @return {?}
     */
    function () {
        return {
            ngModule: SelectComponentModule,
            providers: []
        };
    };
    return SelectComponentModule;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/** @enum {number} */
var TypeColumn = {
    string: 0,
    number: 1,
    date: 2,
    object: 3,
    array: 4,
    default: 5,
    action: 6,
};
TypeColumn[TypeColumn.string] = "string";
TypeColumn[TypeColumn.number] = "number";
TypeColumn[TypeColumn.date] = "date";
TypeColumn[TypeColumn.object] = "object";
TypeColumn[TypeColumn.array] = "array";
TypeColumn[TypeColumn.default] = "default";
TypeColumn[TypeColumn.action] = "action";

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/** @enum {number} */
var TypeOperation = {
    avg: 0,
    sum: 1,
    min: 2,
    max: 3,
};
TypeOperation[TypeOperation.avg] = "avg";
TypeOperation[TypeOperation.sum] = "sum";
TypeOperation[TypeOperation.min] = "min";
TypeOperation[TypeOperation.max] = "max";

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/** @enum {number} */
var TypeFormat = {
    default: 0,
    //without format
    date: 1,
    decimal: 2,
    currency: 3,
    percent: 4,
};
TypeFormat[TypeFormat.default] = "default";
TypeFormat[TypeFormat.date] = "date";
TypeFormat[TypeFormat.decimal] = "decimal";
TypeFormat[TypeFormat.currency] = "currency";
TypeFormat[TypeFormat.percent] = "percent";

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/**
 * Modelo de classe utilizado para inserção de itens no componente totvs-table
 */

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
/**
 * \@widthPixel <br/><br/>
 *
 * Este atributo é inicializado com o valor 768. Tamanho padrão do principal dispositivo que usará o componente.
 * Este atributo é opcional caso alterado deverá receber valores do tipo `number`.
 *
 * \@columns <br/><br/>
 * Este parametro é responsável por receber a lista contendo o cabeçalho da tabela. Esta propriedade deverá receber um valor
 * do tipo `ColumnModel[]`.
 *
 * \@items <br/><br/>
 * Este parametro é responsável por receber a lista dos itens (linhas) da tabela. Esta propriedade  deve receber um
 * valor do tipo `ItemModel[]`.
 *
 * \@filterOption <br/><br/>
 * Este parametro é responsável por definir qual será o filtro inicializado por padrão. Esta propriedade é obrigatoria caso se utilize alguma coluna do tipo filtravel.
 * Esta propriedade deverá receber um valor `string`
 *
 * ### Exemplo HTML 5
 * --------------------------------------------------------------------------------------------
 *   `<totvs-table [columns]="columns" [items]="products" [filterOption]="'name'"></totvs-table>`
 * --------------------------------------------------------------------------------------------
 *
 * ### Exemplo TS
 *
 * -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 *   `columns: ColumnModel[] = [{ label: 'Id', visible: true, attribute: 'id', filterable: false, config: { type: TypeColumn.string } },{label: 'Name Product', visible: true, attribute: 'name', filterable: true, config: { type: TypeColumn.string } }];`
 * -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 *   `products: ItemModel[] = [{id: 1, name: 'Iphone 6',}, {id: 2, name: 'Iphone 7',}, {id: 3, name: 'Sansung Galaxy S7',}, {id: 4, name: 'Sansung Galaxy S8',}];`   *
 * -----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------
 */
var TableComponent = /** @class */ (function () {
    /**
     * Construtor do componente
     * @param platform Injeção de dependencia responsável por manipular os metodos de tamanho de tela (screen resolution)
     * @param modalCtrl Injeção de dependencia responsavel por manipular os metodos de injeção de parametro modal.
     */
    function TableComponent(platform, modalCtrl, format) {
        this.platform = platform;
        this.modalCtrl = modalCtrl;
        this.format = format;
        console.log('Hello TotvsTableComponent Component');
    }
    /**
     * Metódo responsável por retornar o item clicado para o componente pai
     * @param item parametro do tipo `ItemModel` que e passado para esta Metodo para que possa exibir ou ocultar os detalhes do item.
     */
    /**
     * Metódo responsável por retornar o item clicado para o componente pai
     * @param {?} item parametro do tipo `ItemModel` que e passado para esta Metodo para que possa exibir ou ocultar os detalhes do item.
     * @return {?}
     */
    TableComponent.prototype.select = /**
     * Metódo responsável por retornar o item clicado para o componente pai
     * @param {?} item parametro do tipo `ItemModel` que e passado para esta Metodo para que possa exibir ou ocultar os detalhes do item.
     * @return {?}
     */
    function (item) {
        if (item.clicked) {
            item.clicked = false;
        }
        else {
            this.getPaginateItems().forEach(function (element) {
                element.clicked = false;
            });
            item.clicked = true;
        }
        this.onSelect.emit(item);
    };
    /**
     * Metodo responsável por ocultar/exibir os detalhes do item.
     * @param item parametro do tipo `ItemModel` que e passado para esta Metodo para que possa exibir ou ocultar os detalhes do item.
     */
    /**
     * Metodo responsável por ocultar/exibir os detalhes do item.
     * @param {?} item parametro do tipo `ItemModel` que e passado para esta Metodo para que possa exibir ou ocultar os detalhes do item.
     * @return {?}
     */
    TableComponent.prototype.showDetails = /**
     * Metodo responsável por ocultar/exibir os detalhes do item.
     * @param {?} item parametro do tipo `ItemModel` que e passado para esta Metodo para que possa exibir ou ocultar os detalhes do item.
     * @return {?}
     */
    function (item) {
        // item.actions.show_detail = !item.actions.show_detail;
        item.show_detail = !item.show_detail;
    };
    /**
     * Metodo  responsável por verificar se o tamanho da tela é menor ou igual ao valor definido na variável **this.widthPixel**.
     */
    /**
     * Metodo  responsável por verificar se o tamanho da tela é menor ou igual ao valor definido na variável **this.widthPixel**.
     * @return {?}
     */
    TableComponent.prototype.formatTableScreen = /**
     * Metodo  responsável por verificar se o tamanho da tela é menor ou igual ao valor definido na variável **this.widthPixel**.
     * @return {?}
     */
    function () {
        return this.platform.width() <= this.widthPixel;
    };
    /**
     * Metodo responsável por filtrar apenas as `ColumnModel[]` que possuem o valor **visible = true** ou seja somente colunas que serão visiveis.
     * @param visible Parametro responsavel por indicar se retornara as colunas visivel (true) ou não (false).
     * @param actions Parametro responsavel por validar se a coluna retornara actions (true) ou não (false).
     */
    /**
     * Metodo responsável por filtrar apenas as `ColumnModel[]` que possuem o valor **visible = true** ou seja somente colunas que serão visiveis.
     * @param {?=} visible Parametro responsavel por indicar se retornara as colunas visivel (true) ou não (false).
     * @param {?=} actions Parametro responsavel por validar se a coluna retornara actions (true) ou não (false).
     * @return {?}
     */
    TableComponent.prototype.filterColumnVisible = /**
     * Metodo responsável por filtrar apenas as `ColumnModel[]` que possuem o valor **visible = true** ou seja somente colunas que serão visiveis.
     * @param {?=} visible Parametro responsavel por indicar se retornara as colunas visivel (true) ou não (false).
     * @param {?=} actions Parametro responsavel por validar se a coluna retornara actions (true) ou não (false).
     * @return {?}
     */
    function (visible, actions) {
        var /** @type {?} */ result = [];
        if (visible === undefined) {
            visible = true;
        }
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (this.columns[i].visible === visible && this.columns[i].config.type !== TypeColumn.action && this.columns[i].config.type !== TypeColumn.array) {
                result.push(this.columns[i]);
            }
        }
        if (this.hasAction() && actions) {
            var /** @type {?} */ flag = false;
            for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
                if (this.columns[i].config.type === TypeColumn.action) {
                    result.push(this.columns[i]);
                    flag = true;
                }
            }
            if (!flag) {
                result.push({ label: 'Action', attribute: 'action', visible: true, config: { type: TypeColumn.action } });
            }
        }
        return result;
    };
    /**
     * Metodo responsável por filtrar se o tipo da coluna é array.
     * @param column Parametro do tipo `ColumnModel` cujo a responsabilidade é passar os dados do objeto coluna para a Metodo realizar a verificação.
     */
    /**
     * Metodo responsável por filtrar se o tipo da coluna é array.
     * @param {?} column Parametro do tipo `ColumnModel` cujo a responsabilidade é passar os dados do objeto coluna para a Metodo realizar a verificação.
     * @return {?}
     */
    TableComponent.prototype.isColumnArray = /**
     * Metodo responsável por filtrar se o tipo da coluna é array.
     * @param {?} column Parametro do tipo `ColumnModel` cujo a responsabilidade é passar os dados do objeto coluna para a Metodo realizar a verificação.
     * @return {?}
     */
    function (column) {
        if (column.visible === false) {
            if (column.config.type === TypeColumn.array) {
                return true;
            }
        }
        return false;
    };
    /**
     * Metodo responsavel por filtrar se a coluna possui uma ação ou não.
     */
    /**
     * Metodo responsavel por filtrar se a coluna possui uma ação ou não.
     * @return {?}
     */
    TableComponent.prototype.filterColumnAction = /**
     * Metodo responsavel por filtrar se a coluna possui uma ação ou não.
     * @return {?}
     */
    function () {
        var /** @type {?} */ result = [];
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (this.columns[i].config.type === TypeColumn.action) {
                var /** @type {?} */ actions = this.columns[i].config.actions;
                if (Array.isArray(actions)) {
                    for (var /** @type {?} */ j = 0; j < actions.length; j++) {
                        result.push(actions[j]);
                    }
                }
            }
        }
        return result;
    };
    /**
     * Metodo responsável por filtrar os totalizadores das colunas
     */
    /**
     * Metodo responsável por filtrar os totalizadores das colunas
     * @return {?}
     */
    TableComponent.prototype.filterColumnTotal = /**
     * Metodo responsável por filtrar os totalizadores das colunas
     * @return {?}
     */
    function () {
        var /** @type {?} */ result = [];
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (this.columns[i].config.total) {
                if (this.columns[i].config.type !== TypeColumn.number) {
                    throw new Error('Total on type wrong column');
                }
                result.push(this.columns[i]);
            }
        }
        return result;
    };
    /**
     * Metodo responsável por verificar se em todas as colunas existe alguma que possui detalhes
     */
    /**
     * Metodo responsável por verificar se em todas as colunas existe alguma que possui detalhes
     * @return {?}
     */
    TableComponent.prototype.hasDetail = /**
     * Metodo responsável por verificar se em todas as colunas existe alguma que possui detalhes
     * @return {?}
     */
    function () {
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (!this.columns[i].visible) {
                return true;
            }
        }
        return false;
    };
    /**
     * Metodo responsável por verificar se existe uma ação
     * @param action Parametro do tipo `string` que diz a Metodo qual é a ação que ele devera checar se existe
     */
    /**
     * Metodo responsável por verificar se existe uma ação
     * @param {?=} action Parametro do tipo `string` que diz a Metodo qual é a ação que ele devera checar se existe
     * @return {?}
     */
    TableComponent.prototype.hasAction = /**
     * Metodo responsável por verificar se existe uma ação
     * @param {?=} action Parametro do tipo `string` que diz a Metodo qual é a ação que ele devera checar se existe
     * @return {?}
     */
    function (action) {
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (action) {
                if (this.columns[i].config.type === TypeColumn.action) {
                    var /** @type {?} */ actions = this.columns[i].config.actions;
                    if (Array.isArray(actions)) {
                        for (var /** @type {?} */ j = 0; j < actions.length; j++) {
                            if (actions[j].name === action) {
                                return true;
                            }
                        }
                    }
                }
            }
            else if (this.columns[i].config.type === TypeColumn.action || this.hasDetail()) {
                return true;
            }
        }
        return false;
    };
    /**
     * Metodo responsável por executar uma determinada ação atraves dos parametros que lhe serão fornecidos
     * @param action Parametro do tipo `string` que diz a Metodo qual ação ela deverá executar
     * @param item  Parametro do tipo `any` que sera utilizado como passagem de item de uma tela para outra.
     */
    /**
     * Metodo responsável por executar uma determinada ação atraves dos parametros que lhe serão fornecidos
     * @param {?} action Parametro do tipo `string` que diz a Metodo qual ação ela deverá executar
     * @param {?=} item  Parametro do tipo `any` que sera utilizado como passagem de item de uma tela para outra.
     * @return {?}
     */
    TableComponent.prototype.executeAction = /**
     * Metodo responsável por executar uma determinada ação atraves dos parametros que lhe serão fornecidos
     * @param {?} action Parametro do tipo `string` que diz a Metodo qual ação ela deverá executar
     * @param {?=} item  Parametro do tipo `any` que sera utilizado como passagem de item de uma tela para outra.
     * @return {?}
     */
    function (action, item) {
        var /** @type {?} */ modal = this.modalCtrl.create(action.component, { item: item });
        modal.present();
    };
    /**
     *  Metodo responsavel por obter o nome da ação a ser exibida
     */
    /**
     *  Metodo responsavel por obter o nome da ação a ser exibida
     * @return {?}
     */
    TableComponent.prototype.getLabelAction = /**
     *  Metodo responsavel por obter o nome da ação a ser exibida
     * @return {?}
     */
    function () {
        var /** @type {?} */ result = 'Action';
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (this.columns[i].attribute === 'action') {
                result = this.columns[i].label;
            }
        }
        return result;
    };
    /**
     * Metodo resposavel por verificar se dentro das colunas existe alguma que e totalizadora
     */
    /**
     * Metodo resposavel por verificar se dentro das colunas existe alguma que e totalizadora
     * @return {?}
     */
    TableComponent.prototype.hasTotal = /**
     * Metodo resposavel por verificar se dentro das colunas existe alguma que e totalizadora
     * @return {?}
     */
    function () {
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (this.columns[i].config.total) {
                if (this.columns[i].config.type !== TypeColumn.number) {
                    throw new Error('Total on type wrong column');
                }
                return true;
            }
        }
        return false;
    };
    /**
     * Metodo responsável por realizar a soma de todos os itens contidos dentro da coluna
     * @param column Parametro responsavel por informar ao metodo qual é a coluna que realizará o totalizador de soma
     */
    /**
     * Metodo responsável por realizar a soma de todos os itens contidos dentro da coluna
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará o totalizador de soma
     * @return {?}
     */
    TableComponent.prototype.sumFunction = /**
     * Metodo responsável por realizar a soma de todos os itens contidos dentro da coluna
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará o totalizador de soma
     * @return {?}
     */
    function (column) {
        var /** @type {?} */ result = 0;
        var /** @type {?} */ items = this.filteredItems;
        for (var /** @type {?} */ i = 0; i < items.length; i++) {
            result += items[i][column.attribute];
        }
        return result;
    };
    /**
   * Metodo responsável por realizar a media de todos os itens contidos dentro da coluna
   * @param column Parametro responsavel por informar ao metodo qual é a coluna que realizará o totalizador da media
   */
    /**
     * Metodo responsável por realizar a media de todos os itens contidos dentro da coluna
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará o totalizador da media
     * @return {?}
     */
    TableComponent.prototype.avgFunction = /**
     * Metodo responsável por realizar a media de todos os itens contidos dentro da coluna
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará o totalizador da media
     * @return {?}
     */
    function (column) {
        var /** @type {?} */ result = 0;
        var /** @type {?} */ sum = 0;
        var /** @type {?} */ items = this.filteredItems;
        for (var /** @type {?} */ i = 0; i < items.length; i++) {
            sum += items[i][column.attribute];
        }
        result = sum / items.length;
        return result;
    };
    /**
   * Metodo responsável por realizar a busca do maior valor contido dentro da lista de itens
   * @param column Parametro responsavel por informar ao metodo qual é a coluna que realizará busca do maior valor
   */
    /**
     * Metodo responsável por realizar a busca do maior valor contido dentro da lista de itens
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará busca do maior valor
     * @return {?}
     */
    TableComponent.prototype.maxFunction = /**
     * Metodo responsável por realizar a busca do maior valor contido dentro da lista de itens
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará busca do maior valor
     * @return {?}
     */
    function (column) {
        var /** @type {?} */ start = true;
        var /** @type {?} */ result;
        var /** @type {?} */ items = this.filteredItems;
        for (var /** @type {?} */ i = 0; i < items.length; i++) {
            if (start === true) {
                start = false;
                result = items[i][column.attribute];
            }
            else {
                if (result < items[i][column.attribute]) {
                    result = items[i][column.attribute];
                }
            }
        }
        return result;
    };
    /**
   * Metodo responsável por realizar a busca do menor valor contido dentro da lista de itens
   * @param column Parametro responsavel por informar ao metodo qual é a coluna que realizará busca do menor valor
   */
    /**
     * Metodo responsável por realizar a busca do menor valor contido dentro da lista de itens
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará busca do menor valor
     * @return {?}
     */
    TableComponent.prototype.minFunction = /**
     * Metodo responsável por realizar a busca do menor valor contido dentro da lista de itens
     * @param {?} column Parametro responsavel por informar ao metodo qual é a coluna que realizará busca do menor valor
     * @return {?}
     */
    function (column) {
        var /** @type {?} */ start = true;
        var /** @type {?} */ result;
        var /** @type {?} */ items = this.filteredItems;
        for (var /** @type {?} */ i = 0; i < items.length; i++) {
            if (start === true) {
                start = false;
                result = items[i][column.attribute];
            }
            else {
                if (result > items[i][column.attribute]) {
                    result = items[i][column.attribute];
                }
            }
        }
        return result;
    };
    /**
     * Metodo responsavel por atribuir um valor de operação dentro dos totalizadores das colunas
     * @param column Parametro responsavel por informar ao metodo qual coluna receberá o totalizador
     */
    /**
     * Metodo responsavel por atribuir um valor de operação dentro dos totalizadores das colunas
     * @param {?} column Parametro responsavel por informar ao metodo qual coluna receberá o totalizador
     * @return {?}
     */
    TableComponent.prototype.getTotalColumn = /**
     * Metodo responsavel por atribuir um valor de operação dentro dos totalizadores das colunas
     * @param {?} column Parametro responsavel por informar ao metodo qual coluna receberá o totalizador
     * @return {?}
     */
    function (column) {
        var /** @type {?} */ result = 0;
        switch (column.config.total) {
            case TypeOperation.sum:
                this.totalizer = 'Total Sum';
                result = this.sumFunction(column);
                return result;
            case TypeOperation.avg:
                this.totalizer = 'Simple average';
                result = this.avgFunction(column);
                return result;
            case TypeOperation.max:
                this.totalizer = 'Maximum value';
                result = this.maxFunction(column);
                return result;
            case TypeOperation.min:
                this.totalizer = 'Minimum value';
                result = this.minFunction(column);
                return result;
        }
    };
    /**
     * Metodo responsavel por checar se existe um filtro de busca no componente
     */
    /**
     * Metodo responsavel por checar se existe um filtro de busca no componente
     * @return {?}
     */
    TableComponent.prototype.haveResearch = /**
     * Metodo responsavel por checar se existe um filtro de busca no componente
     * @return {?}
     */
    function () {
        return this.filterColumnSearch().length > 0;
    };
    /**
     * Metodo responsavel por filtrar as colunas que são filtraveis
     */
    /**
     * Metodo responsavel por filtrar as colunas que são filtraveis
     * @return {?}
     */
    TableComponent.prototype.filterColumnSearch = /**
     * Metodo responsavel por filtrar as colunas que são filtraveis
     * @return {?}
     */
    function () {
        var /** @type {?} */ result = [];
        for (var /** @type {?} */ i = 0; i < this.columns.length; i++) {
            if (this.columns[i].filterable) {
                if (this.filterOption === '' || this.filterOption === undefined) {
                    this.filterOption = this.columns[i].attribute;
                }
                result.push(this.columns[i]);
            }
        }
        return result;
    };
    /**
     * @param {?} value
     * @return {?}
     */
    TableComponent.prototype.filterItems = /**
     * @param {?} value
     * @return {?}
     */
    function (value) {
        var _this = this;
        if (!value) {
            this.filteredItems = this.items;
        }
        else {
            this.filteredItems = this.items.filter(function (d) {
                var /** @type {?} */ column = _this.getColumnAttribute(_this.filterOption);
                if (column.config.type === TypeColumn.object) {
                    var /** @type {?} */ config = column.config;
                    return d[_this.filterOption][config.attribute_object].toString().toLowerCase().indexOf(value.toString().toLowerCase()) >= 0;
                }
                else {
                    return d[_this.filterOption].toString().toLowerCase().indexOf(value.toString().toLowerCase()) >= 0;
                }
            });
        }
    };
    /**
     * @param {?} attribute
     * @return {?}
     */
    TableComponent.prototype.getColumnAttribute = /**
     * @param {?} attribute
     * @return {?}
     */
    function (attribute) {
        this.columns.forEach(function (element) {
            if (element.attribute === attribute) {
                return element;
            }
        });
        throw new Error('Column not found.');
    };
    /**
     * Metodo responsavel por obter os valores contidos no array de items de acordo com as colunas.
     * @param column Parametro responsável por informar qual a coluna pertencente ao valor.
     * @param item Parametro responsavel por informar qual valor pertence a coluna.
     */
    /**
     * Metodo responsavel por obter os valores contidos no array de items de acordo com as colunas.
     * @param {?} column Parametro responsável por informar qual a coluna pertencente ao valor.
     * @param {?} item Parametro responsavel por informar qual valor pertence a coluna.
     * @return {?}
     */
    TableComponent.prototype.getValues = /**
     * Metodo responsavel por obter os valores contidos no array de items de acordo com as colunas.
     * @param {?} column Parametro responsável por informar qual a coluna pertencente ao valor.
     * @param {?} item Parametro responsavel por informar qual valor pertence a coluna.
     * @return {?}
     */
    function (column, item) {
        switch (column.config.type) {
            case TypeColumn.object:
                var /** @type {?} */ config = column.config;
                return (column.config.format) ? this.getValueFormated(column.config.format, item[column.attribute][config.attribute_object]) : item[column.attribute][config.attribute_object];
            default:
                return (column.config.format) ? this.getValueFormated(column.config.format, item[column.attribute]) : item[column.attribute];
        }
    };
    /**
     * @param {?} format
     * @param {?} value
     * @return {?}
     */
    TableComponent.prototype.getValueFormated = /**
     * @param {?} format
     * @param {?} value
     * @return {?}
     */
    function (format, value) {
        this.format.setValue(value);
        switch (format.type) {
            case TypeFormat.currency:
                return this.format.toCurrency(format.options.currencyCode, format.options.displaySymbol, format.options.digits);
            case TypeFormat.percent:
                return this.format.toPercent(format.options.digits);
            case TypeFormat.decimal:
                return this.format.toDecimal(format.options.digits);
            case TypeFormat.date:
                return this.format.toDateFormated(format.options.pattern);
            default:
                return this.format.toString;
        }
    };
    //todo: mecher neste metodo
    // reorderList() {
    //   for (let i = 0; i < this.itemsAdded.length; i++) {
    //     let item = this.itemsAdded[i];
    //     item.inList = { order: i + 1 }
    //   }
    // }
    /**
     * @return {?}
     */
    TableComponent.prototype.getPaginateItems = /**
     * @return {?}
     */
    function () {
        if (this.hasPagination()) {
            this.numberPages = Math.ceil(this.filteredItems.length / this.pagination);
            var /** @type {?} */ start = (this.currentPage - 1) * this.pagination;
            var /** @type {?} */ finish = (this.currentPage * this.pagination);
            return this.filteredItems.slice(start, finish);
        }
        else {
            return this.filteredItems;
        }
    };
    /**
     * Metodo responsável por retornar se exite o serviço de paginação do totvs-table
     *
     */
    /**
     * Metodo responsável por retornar se exite o serviço de paginação do totvs-table
     *
     * @return {?}
     */
    TableComponent.prototype.hasPagination = /**
     * Metodo responsável por retornar se exite o serviço de paginação do totvs-table
     *
     * @return {?}
     */
    function () {
        if (this.pagination && this.pagination < this.filteredItems.length) {
            return true;
        }
        else {
            return false;
        }
    };
    /**
       * Metodo responsável por exibir a pagina atual na view
       */
    /**
     * Metodo responsável por exibir a pagina atual na view
     * @return {?}
     */
    TableComponent.prototype.showCurrentPage = /**
     * Metodo responsável por exibir a pagina atual na view
     * @return {?}
     */
    function () {
        var /** @type {?} */ page;
        page = (this.currentPage).toString();
        return page;
    };
    /**
     * Metodo responsável por retornar a view a quantidade total de paginas existentes
     */
    /**
     * Metodo responsável por retornar a view a quantidade total de paginas existentes
     * @return {?}
     */
    TableComponent.prototype.shownumberPages = /**
     * Metodo responsável por retornar a view a quantidade total de paginas existentes
     * @return {?}
     */
    function () {
        return (this.numberPages).toString();
    };
    /**Metodo responsável por avançar a paginação > */
    /**
     * Metodo responsável por avançar a paginação >
     * @return {?}
     */
    TableComponent.prototype.nextPage = /**
     * Metodo responsável por avançar a paginação >
     * @return {?}
     */
    function () {
        if (this.currentPage < (this.numberPages)) {
            this.currentPage++;
        }
        else {
            this.currentPage = (this.numberPages);
        }
    };
    /**
     * Metodo responsável por retroceder a paginação <
     */
    /**
     * Metodo responsável por retroceder a paginação <
     * @return {?}
     */
    TableComponent.prototype.previousPage = /**
     * Metodo responsável por retroceder a paginação <
     * @return {?}
     */
    function () {
        if (this.currentPage > 1) {
            this.currentPage--;
        }
        else {
            this.currentPage = 1;
        }
    };
    /**
     * @return {?}
     */
    TableComponent.prototype.ngOnInit = /**
     * @return {?}
     */
    function () {
        //Called after the constructor, initializing input properties, and the first call to ngOnChanges.
        //Add 'implements OnInit' to the class.
        if (this.filteredItems.length === 0) {
            this.filteredItems = this.items;
        }
    };
    return TableComponent;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var TableComponentModule = /** @class */ (function () {
    function TableComponentModule() {
    }
    /**
     * @return {?}
     */
    TableComponentModule.forRoot = /**
     * @return {?}
     */
    function () {
        return {
            ngModule: TableComponentModule,
            providers: []
        };
    };
    return TableComponentModule;
}());

/**
 * @fileoverview added by tsickle
 * @suppress {checkTypes} checked by tsc
 */
var TotvsCustomModule = /** @class */ (function () {
    function TotvsCustomModule() {
    }
    /**
     * @return {?}
     */
    TotvsCustomModule.forRoot = /**
     * @return {?}
     */
    function () {
        return {
            ngModule: TotvsCustomModule,
            providers: [AppProvider, FormaterProvider, DateProvider]
        };
    };
    return TotvsCustomModule;
}());

export { TotvsCustomModule, createTranslateLoader, SharedModule, HeaderComponentModule, SelectComponentModule, TableComponentModule, HeaderComponent, SelectComponent, TableComponent, AppProvider, DateProvider, FormaterProvider };
