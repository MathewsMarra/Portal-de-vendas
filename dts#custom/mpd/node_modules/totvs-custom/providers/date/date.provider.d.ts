import { DateModel } from './date.model';
export declare class DateProvider {
    private now;
    private nowDate;
    constructor(date?: Date);
    format(date: Date): DateModel;
    private formatDay(day);
    private formatMonth(month);
    private formatHour(hour);
    private formatMinute(minute);
    private formatSecond(second);
    getDateModel(): DateModel;
    getDateFormated(format: string): string;
}
