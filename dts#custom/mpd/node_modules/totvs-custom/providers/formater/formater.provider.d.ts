import { CurrencyPipe, DecimalPipe, PercentPipe, DatePipe } from '@angular/common';
export declare class FormaterProvider {
    private currencyPipe;
    private decimalPipe;
    private percentPipe;
    private datePipe;
    private value;
    constructor(currencyPipe: CurrencyPipe, decimalPipe: DecimalPipe, percentPipe: PercentPipe, datePipe: DatePipe);
    setValue(value: any): void;
    toString(): string;
    toInt(): number;
    toFloat(fixed?: number): number;
    toCurrency(currencyCode?: string, symbolDisplay?: boolean, digits?: string): string | null;
    toDecimal(digits?: string): string | null;
    toPercent(digits?: string): string | null;
    toDateFormated(pattern?: string): string | null;
}
