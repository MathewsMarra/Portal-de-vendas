import { App, Platform, ToastController, AlertController, ModalController, LoadingController } from 'ionic-angular';
import { Storage } from '@ionic/storage';
import { Network } from '@ionic-native/network';
import { Device } from '@ionic-native/device';
import { ListMenuModel, InfoModel, ConfigModel, DataModel } from './app.model';
import { UserModel } from '../user/user.model';
export declare class AppProvider {
    private app;
    private platform;
    private loadingCtrl;
    private storage;
    private network;
    private toastCtrl;
    private alertCtrl;
    private modalCtrl;
    private device;
    private database;
    private databaseDriverOrder;
    private table;
    private loading;
    private datePrvd;
    private info;
    private menu;
    constructor(app: App, platform: Platform, loadingCtrl: LoadingController, storage: Storage, network: Network, toastCtrl: ToastController, alertCtrl: AlertController, modalCtrl: ModalController, device: Device);
    runInBrowser(): boolean;
    isTest(): boolean;
    getInfoDevice(): Device;
    getInfoFormater(): {
        currency_code: string;
        currency_expression: string;
        number_decimal: string;
    };
    is(key: string): boolean;
    getInfo(): InfoModel;
    getConfig(): ConfigModel;
    getDebugInfo(): string;
    isSync(): boolean;
    getDate(): string;
    getDateFormated(format: string): string;
    getUrlProxy(): string;
    getUrl(resource: string, type?: string, action?: boolean): string;
    getConnection(): string;
    infoNetwork(): void;
    isOnline(): boolean;
    getDatabase(): string;
    getDatabaseDriverOrder(): string[];
    setUser(user?: UserModel): void;
    setCompany(company: string): void;
    getUser(): UserModel;
    guest(): UserModel;
    isGuest(): boolean;
    crypt(key: string, pass: string): string;
    logout(): void;
    setData(data: DataModel): void;
    getData(data: DataModel): Promise<any>;
    setMenu(menu: ListMenuModel): void;
    getMenu(): ListMenuModel;
    private load();
    private save();
    presentAlert(title: string, msg: string): void;
    presentToast(msg: string): void;
    presentModal(page: any, params?: any, fn?: any): void;
    startLoading(): void;
    stopLoading(): void;
    /**
     * @description Função para converter XML em JSON, realizada alteração para lelet em consideração ou não os atributos e inputar no objeto também os valores dos nós
     * @author David Walsh
     * @source https://davidwalsh.name/convert-xml-json
     */
    parseToJSON(xml: any, attr?: boolean): any;
}
